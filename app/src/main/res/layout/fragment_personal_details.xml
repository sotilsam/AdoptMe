<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/app_bg">

    <include
        android:id="@+id/topBar"
        layout="@layout/include_top_bar_back_title"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <TextView
        android:id="@+id/tvNameLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="name"
        android:textColor="@color/text_white"
        android:textSize="28sp"
        app:layout_constraintTop_toBottomOf="@id/topBar"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginStart="@dimen/page_padding"
        android:layout_marginTop="80dp"/>

    <TextView
        android:id="@+id/tvEmailLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="email"
        android:textColor="@color/text_white"
        android:textSize="28sp"
        app:layout_constraintTop_toBottomOf="@id/tvNameLabel"
        app:layout_constraintStart_toStartOf="@id/tvNameLabel"
        android:layout_marginTop="16dp"/>

    <TextView
        android:id="@+id/tvPhoneLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="phone"
        android:textColor="@color/text_white"
        android:textSize="28sp"
        app:layout_constraintTop_toBottomOf="@id/tvEmailLabel"
        app:layout_constraintStart_toStartOf="@id/tvNameLabel"
        android:layout_marginTop="16dp"/>

    <TextView
        android:id="@+id/tvHouseKids"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="house with kids"
        android:textColor="@color/text_white"
        android:textSize="28sp"
        app:layout_constraintTop_toBottomOf="@id/tvPhoneLabel"
        app:layout_constraintStart_toStartOf="@id/tvNameLabel"
        android:layout_marginTop="40dp"/>

    <TextView
        android:id="@+id/tvHouseNoKids"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="house without kids"
        android:textColor="@color/text_white"
        android:textSize="28sp"
        app:layout_constraintTop_toBottomOf="@id/tvHouseKids"
        app:layout_constraintStart_toStartOf="@id/tvNameLabel"
        android:layout_marginTop="16dp"/>


</androidx.constraintlayout.widget.ConstraintLayout>
